import contactRoute from '../features/contacts/contactsPage/contactRoute';
import authRoute from '../features/auth/page/route';

const appRoutes = {
  auth: authRoute,
  contacts: contactRoute,
}

export default appRoutes;
