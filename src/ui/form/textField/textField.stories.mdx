import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TextField } from './TextField';
import { DefaultUse, InValidState, ValidState } from './textField.stories';

<Meta title="UI/Form/TextField" component={ TextField }/>

# TextField

Simple `<form />` text field. With validation states support.

<Canvas>
  <Story story={ DefaultUse } />
  <Story story={ ValidState } />
  <Story story={ InValidState } />
</Canvas>

<ArgsTable of={ TextField } />
